-- Phase 14 algebraic-effects example.
-- This file stays close to the current parser-compatible core syntax.

transform announceWorker =
  perform IO.println("worker starting")

transform sendPing target =
  perform Process.send(target, "ping")

transform readSelf =
  perform Process.self()

transform runWorker target =
  perform IO.println("dispatching async work")
