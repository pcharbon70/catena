# Specs Planning Index

This directory holds planning documents that are canonical enough to promote out of `notes/planning/`.

Current promoted planning view:

- [current_status.md](current_status.md)

The detailed phase documents still live under `notes/planning/`. This directory should only hold the reconciled view that contributors need in order to understand what is actually implemented versus still planned.
